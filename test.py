import mysql.connector as mysql_
"""
db = mysql_.connect(
    host="localhost",
    user="root",
    password="",
    database="hotweb"
)
curr  = db.cursor()

sql = "INSERT INTO hotweb(name,username) VALUES (%s,%s)"
vals=("manlow","realManlows")
curr.execute(sql,vals)
db.commit()
"""
